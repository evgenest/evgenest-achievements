# Copy to .dev.vars for local development. Production values go through `wrangler secret put`.
GITHUB_TOKEN=""
GITHUB_REPORTS_TOKEN=""
OPENAI_API_KEY=""
VERCEL_AI_GATEWAY_API_KEY=""
OPENROUTER_API_KEY=""
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
RUN_SECRET=""
# Free-form developer profile fed to the model (stack, seniority, region).
# Kept out of the repo on purpose: it is personal data and it drives the salary estimate.
# If it grows past a one-liner, write it as a gitignored DEV_PROFILE.md and run
# `bun run sync:dev-profile` — see README "Configuration".
DEV_PROFILE=""
